rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TradingAgents accesses Firestore only from the Python server through
    // Firebase Admin SDK. Never expose this database directly to a browser.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
